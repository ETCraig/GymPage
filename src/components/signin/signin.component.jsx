import React from 'react';
import {
    SignInContainer,
    SignInTitle,
    ButtonsBarContainer
} from './signin.styles';

import { connect } from 'react-redux';

const SignIn = ({ }) => {
    return (
        <SignInContainer>

        </SignInContainer>
    );
}

export default SignIn;